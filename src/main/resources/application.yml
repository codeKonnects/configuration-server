server:
  port: 9808

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/codeKonnects/configuration-server
          clone-on-start: true
